#ifndef MELEE_MATH_H
#define MELEE_MATH_H

#define ABS(x)      ((x)<0 ? -(x) : (x))
#define SIGN(x)    (((x) >= 0) - (x < 0))
#define MML_PI 3.14159265359

float mml_sin(float);
float mml_cos(float);

#endif
